# Standard Web export profile

- Output one complete HTML5 document.
- Preserve the article's semantic structure and safe inline presentation.
- Remove scripts, frames, forms, `<base>`, event handlers, and unsafe URLs.
- Add immutable Galley provenance metadata in `<head>`.
- Never modify the source `*.galley.html` document.
